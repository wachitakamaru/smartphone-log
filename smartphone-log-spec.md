# スマホ利用ログ 仕様設計書

- 対象ファイル: `smartphone-log.html`（単一HTML、外部依存なし）
- 版: v2（2026-09-13）
- 配置先: GitHub Pages

## 1. 目的と背景

スマホの利用時間を「多い／少ない」で評価するのではなく、「目的があって見た時間」と「惰性で見た時間」に分け、後者を削る判断材料を得るための個人用ツール。

iOSショートカットのオートメーションでメモに自動追記された開閉ログを取り込み、セッション単位に整形し、夜に振り返って印を付け、日・アプリ・時間帯の観点で可視化する。

## 2. 動作環境

| 項目 | 内容 |
|---|---|
| 形式 | 単一HTMLファイル（HTML / CSS / JavaScript を内包） |
| 外部ライブラリ | なし。グラフはSVGを自前描画 |
| 想定端末 | iPhone Safari（縦画面、幅 380px 前後）を第一に設計。PCブラウザでも動作 |
| ホスティング | GitHub Pages（静的配信）。サーバ処理なし |
| 通信 | 一切行わない。データは端末内に閉じる |

## 3. 画面構成

ヘッダ（タイトル「スマホ利用ログ」、全消去ボタン）と3つのタブで構成する。

```
┌──────────────────────────────┐
│ スマホ利用ログ        [全消去] │
│ 取り込み │ レビュー │ 集計    │
├──────────────────────────────┤
│ （選択中タブの内容）            │
└──────────────────────────────┘
```

### 3.1 取り込みタブ

| 要素 | 仕様 |
|---|---|
| ログ貼り付け欄 | メモアプリの内容をそのまま貼るテキストエリア |
| 接続しきい値 | 同一アプリの close→open 間隔が何分以内なら1セッションにつなぐか。1／2／3／5分、既定2分 |
| 取り込むボタン | 4章の規則で解析し、既存データへ追加。結果件数を表示 |
| バックアップ | JSON書き出し／読み込み（7章） |
| サンプルデータ | 直近7日分のダミーログを生成し取り込む（動作確認用） |
| 統計カード | 総セッション数、未レビュー件数 |

### 3.2 レビュータブ

| 要素 | 仕様 |
|---|---|
| 日付選択 | データが存在する日のみを新しい順に列挙 |
| セッション一覧 | 時刻範囲、アプリ名、分数を表示 |
| 見た理由 | 「目的があって見た」「惰性で見た」の2択トグル |
| 見た後の気持ち | 「満足」「虚しい」の2択トグル |
| 一言メモ | 任意テキスト |
| 保存ボタン | 画面下部に固定表示。押下時のみ永続化する |

- トグルは画面上の一時状態（draft）を変更するだけで、保存ボタンを押すまで書き込まない（件数が多い場合の反応速度対策）。
- 同じボタンを再度押すと選択解除。
- 保存後に「N件を保存しました」を表示。

### 3.3 集計タブ

上から順に以下を表示する。対象期間は「今日を含む直近7日」。

| 区画 | 内容 |
|---|---|
| タイムライン | 横軸=日付（7列）、縦軸=時刻 0:00〜24:00。各セッションをアプリ色の縦帯で描画 |
| 日ごとの使用時間 | 日別の積み上げ棒。積み上げ要素はアプリ（X / YouTube / TikTok / その他） |
| アプリ別の回数と平均時間 | 表。回数、平均分数、合計分数 |
| 満足と虚しいの時間 | 日別の積み上げ棒。要素は 満足 / 虚しい / 未レビュー。下記フィルタを適用 |
| フィルタ | アプリ4種のチェックボックス（複数選択）、見た理由2種のチェックボックス |
| コメント一覧 | 全期間のセッションを日付降順に表示。理由・気持ちのラベルとメモ。フィルタを適用 |

フィルタの解釈:
- アプリ: チェックされたアプリのみ対象。
- 見た理由: 両方チェック時は未レビュー分も含めて全件。片方のみの場合、その理由が付いたセッションのみ。

## 4. ログ取り込み仕様

### 4.1 入力形式

1行1イベント、カンマ区切り3列。

```
アプリ名,YYYY/MM/DD HH:mm,open|close
```

例:

```
X,2026/09/12 21:00,open
,2026/09/12 21:01,close
,2026/09/12 21:01,close
TikTok,2026/09/12 21:07,open
,2026/09/12 21:11,close
TikTok,2026/09/12 21:11,open
,2026/09/12 21:13,close
```

- close行はアプリ名が空でよい（ショートカット側の制約）。
- 日時の月日時は1桁でも可。秒は不要。
- 3列に満たない行、日時を読めない行は無視する。

### 4.2 アプリ名の正規化

| 入力（大文字小文字不問） | 正規化後 |
|---|---|
| `x`, `twitter` | X |
| `youtube` を含む | YouTube |
| `tiktok`, `ticktock` を含む | TikTok |
| 上記以外 | その他 |

### 4.3 セッション化の規則

1. イベントを日時昇順に並べる。
2. close行のアプリ名は直前のopenのアプリ名で補完する。
3. open → 次のclose を1セッションとする。
4. openの直後にcloseを挟まず別のopenが来た場合、前のopenは次のopenの時刻で閉じる。
5. closeが連続した場合、2つ目以降は無視される（すでに閉じているため）。
6. 同一アプリで、前セッションのcloseから次のopenまでが「接続しきい値」以内なら1セッションに結合する。
7. 最後のopenに対応するcloseがない場合、open時刻と同時刻の1分セッションとして扱う。
8. 分数は `round((end − start) / 60秒)`、最小1分。

### 4.4 重複判定

既存セッションと「アプリ名が同じ、かつ開始時刻の差が60秒未満」のものは追加しない。これによりメモ全体を毎回貼り直しても二重登録されない。

## 5. データモデル

セッション配列 `S` を唯一の正とする。1要素の構造:

| フィールド | 型 | 内容 |
|---|---|---|
| ts | number | 開始時刻（Unixミリ秒） |
| end | number | 終了時刻（Unixミリ秒） |
| day | string | `YYYY-MM-DD`（端末ローカル日付） |
| time | string | `HH:mm` 開始時刻の表示用 |
| app | string | X / YouTube / TikTok / その他 |
| min | number | 分数（1以上） |
| why | string \| null | 「目的」「惰性」または未設定 |
| feel | string \| null | 「満足」「虚しい」または未設定 |
| memo | string | 一言メモ |

日付をまたぐセッションは開始日の `day` に属し、タイムライン上では開始日の24:00までを描画する。

## 6. 永続化

保存キー: `phone-log-v2`。値はセッション配列のJSON文字列。

| 優先順 | 保存先 | 用途 |
|---|---|---|
| 1 | `window.storage`（Claude Artifacts環境） | 存在する場合のみ |
| 2 | `localStorage` | GitHub Pages / Safari で使用される |
| 3 | メモリのみ | 上記が使えない場合。リロードで消える |

### 6.1 GitHub Pages 運用上の注意

- `localStorage` はオリジン（`https://<user>.github.io`）単位で保存される。同一リポジトリ内の別ページとキーが衝突しないよう、キー名にアプリ名を含めている。
- iOS Safariでは、そのサイトを一定期間（目安7日）使わないと `localStorage` が削除されることがある。**「ホーム画面に追加」して起動すると、この削除の対象外になる**ため、ホーム画面からの起動を標準運用とする。
- Safariで開いたものとホーム画面アイコンから開いたものは保存領域が別になる。起動方法を統一すること。
- iCloudは記録データを同期しない。バックアップは7章のJSON書き出しで行う。

## 7. バックアップ（JSON）

### 7.1 書き出し

ファイル名 `smartphone-log-YYYY-MM-DD.json` をダウンロードする。

```json
{
  "app": "smartphone-log",
  "version": 2,
  "exported": "2026-09-13T12:00:00.000Z",
  "sessions": [ { ...5章の構造... } ]
}
```

### 7.2 読み込み

- ファイル選択ダイアログから `.json` を選ぶ。
- 上記形式、またはセッション配列そのものを受け付ける。
- 追加方式。4.4と同じ規則で重複を除外する。
- `ts` と `app` を持たない要素は無視する。欠けている `day` / `time` / `end` は `ts` から補完する。

## 8. 全消去

- ヘッダの「全消去」ボタンから実行。
- 件数を含めた確認ダイアログ「この操作は元に戻せません」を表示し、OK時のみ全セッションを削除して永続化する。
- 記録が0件のときは「記録はありません」と表示して何もしない。

## 9. グラフ描画仕様

すべて `viewBox` 指定のSVGで、コンテナ幅に追従する。

| グラフ | 主要パラメータ |
|---|---|
| タイムライン | viewBox 340×300。縦軸は3時間刻みのグリッド。帯は列幅の60% |
| 積み上げ棒（2種） | viewBox 340×200。縦軸最大値は60分単位で切り上げ、最小60分。グリッド3分割。ラベルは60分以上を「h」表記 |

配色（CSS変数）:

| 変数 | 用途 | 値 |
|---|---|---|
| `--x` | X | #2E3A38 |
| `--yt` | YouTube | #C9705A |
| `--tt` | TikTok | #2F8F86 |
| `--ot` | その他 | #B9C0BC |
| `--purpose` | 目的 / 満足 | #3E7A3A |
| `--idle` | 惰性 / 虚しい | #7E5273 |
| `--none` | 未レビュー | #E9ECE9 |

## 10. GitHub Pages への配置手順

1. リポジトリ直下に `index.html` として配置（`smartphone-log.html` をリネーム）。
2. Settings → Pages → Source を「Deploy from a branch」、対象ブランチとルートを指定。
3. 数分後に `https://<user>.github.io/<repo>/` で公開される。
4. iPhone Safariで開き、共有 →「ホーム画面に追加」。以後はアイコンから起動する。

公開リポジトリの場合、HTML自体は誰でも閲覧できるが、記録データは各端末のブラウザ内にのみ保存され、リポジトリには含まれない。

## 11. 既知の制約

- 「閉じたとき」のオートメーションは他アプリへの切り替えや画面ロックでも発火するため、細切れのイベントが生じる。4.3の接続規則で吸収する。
- ショートカットの制約でclose行にアプリ名が入らない。直前のopenで補完する前提のため、ログの順序が入れ替わると誤ったアプリに紐づく可能性がある。
- 端末のローカル時刻で日付を決めるため、タイムゾーンをまたぐ移動時は日付の帰属がずれる。
- 未レビューのセッションは「満足と虚しいの時間」ではグレーで表示され、理由フィルタを片方だけにすると除外される。

## 12. 今後の拡張候補

- 集計期間の切り替え（7日 / 30日）
- 時間帯ヒートマップ（曜日×時間）
- 目標時間の設定と達成表示
- Web Share API によるメモからの直接取り込み
